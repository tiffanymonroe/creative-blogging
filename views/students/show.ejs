<!DOCTYPE html>
<html>
  <head>
    <% include ../partials/head.ejs %>
  </head>
  <body>
    <% include ../partials/nav.ejs %>
    <div class="container">
      <div class="row">
        <div class="col-xs-12">
          <h1><%=student.firstName%> <%=student.lastName%></h1>
        </div>
        <div class="col-xs-8">
          <img class="show-images img-thumbnail" src="<%=student.img%>"/>
          <table class="table table-responsive table-bordered">
            <tr>
              <th>Major: </th>
              <td> <%=student.major%> </td>
            </tr>
            <tr>
              <th>About Me: </th>
              <td> <%=student.about%> </td>
            </tr>
            <tr>
              <th>I'm taking Intro to Creative Writing because: </th>
              <td> <%=student.course%> </td>
            </tr>
            <tr>
              <th>I'm most excited to write: </th>
              <td> <%=student.genre%> </td>
            </tr>
          </table>
        </div>
        <div class="col-xs-4">
          <h2>Wiki Articles</h2>
          <% for (i = 0; i < student.wiki.title; i++){%>
            <a href="<%=wiki.id[i]%>"><%=student.wiki.title[i]%></a>
          <%}%>
        </div>

        <div class="row">
          <div class="col-xs-12">
              <a class="btn btn-default crud" href="/students/<%=student.id%>/edit">Edit</a>
              <form action="/students/<%=student._id%>?_method=DELETE" method="post">
                <input class="btn btn-default" type="submit" value="Delete"/>
              </form>
          </div>
        </div>

    </div>
  </body>
</html>
