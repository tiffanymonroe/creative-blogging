<!DOCTYPE html>
<html>
  <head>
    <% include ../partials/head.ejs %>
  </head>
  <body>
    <% include ../partials/nav.ejs %>
    <div class="container">
      <div class="add-container col-xs-6 col-xs-offset-3">

        <form action="/wiki" method="post">
          <select name="studentId">
            <% for (let i=0; i < students.length; i++){ %>
              <option value="<%=students[i]._id%>"><%=students[i].firstName%> <%=students[i].lastName%></option>
            <% } %>
          </select>
        <br/>
          <label for="title">Title:</label>
          <input type="text" class="form-control" name="title">
          <label for="article">Article:</label>
          <textarea class="form-control" rows="5" name="article"></textarea>
          <input class="btn btn-info" role="button" type="submit" value="Submit"/>
        </form>
      </div>
    </div>
  </body>
</html>
